{
  "name": "audio-stream-websocket-server",
  "version": "1.0.0",
  "description": "WebSocket relay for audio stream app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.17.1"
  },
  "engines": {
     "node": ">=18.0.0"
  }
}
